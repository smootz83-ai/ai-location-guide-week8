services:
  - type: web
    name: ai-location-guide-week8
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: PUBLIC_DATA_API_KEY
        sync: false
